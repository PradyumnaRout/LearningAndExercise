//
//  CircleRippleEffect.swift
//  LearningAndExercise
//
//  Created by hb on 04/02/26.
//

import Foundation

/*
 
 private var keyImage: some View {
     ZStack {
         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 180 : 30, height: animate ? 180 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                 .repeatForever(autoreverses: false),
                 value: animate
             )
         
         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 130 : 30, height: animate ? 130 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                 .repeatForever(autoreverses: false)
                 .delay(0.3),
                 value: animate
             )
         
         Image(.key)
             .resizable()
             .scaledToFit()
             .frame(width: 80, height: 80)
             .shadow(color: Color(.primaryButton).opacity(0.3), radius: 5, x: 5, y: 5)
     }
     .frame(width: 200, height: 200)
 }
 
 
 private var regionImage: some View {
     ZStack {
         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 180 : 30, height: animate ? 180 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                     .repeatForever(autoreverses: false),
                 value: animate
             )

         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 130 : 30, height: animate ? 130 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                     .repeatForever(autoreverses: false)
                     .delay(0.3),
                 value: animate
             )

         Image(.mapIcon)
             .resizable()
             .scaledToFit()
             .frame(width: 80, height: 80)
             .shadow(color: Color(.primaryButton).opacity(0.3), radius: 5, x: 5, y: 5)
     }
     .frame(width: 200, height: 200)
 }

 
 private var verifyImage: some View {
     ZStack {
         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 180 : 30, height: animate ? 180 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                     .repeatForever(autoreverses: false),
                 value: animate
             )

         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 130 : 30, height: animate ? 130 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                     .repeatForever(autoreverses: false)
                     .delay(0.3),
                 value: animate
             )

         Image(.shield)
             .resizable()
             .scaledToFit()
             .frame(width: 80, height: 80)
             .shadow(color: Color(.primaryButton).opacity(0.3), radius: 5, x: 5, y: 5)
     }
     .frame(width: 200, height: 200)
 }
 
 
 private var biometricImage: some View {
     ZStack {
         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 180 : 30, height: animate ? 180 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                     .repeatForever(autoreverses: false),
                 value: animate
             )

         Circle()
             .stroke(Color.primaryButton.opacity(1.0), lineWidth: 1)
             .frame(width: animate ? 130 : 30, height: animate ? 130 : 30)
             .opacity(animate ? 0 : 1)
             .animation(
                 .easeOut(duration: 1.2)
                     .repeatForever(autoreverses: false)
                     .delay(0.3),
                 value: animate
             )

         Image(.faceVerf)
             .resizable()
             .scaledToFit()
             .frame(width: 80, height: 80)
             .shadow(color: Color(.primaryButton).opacity(0.3), radius: 5, x: 5, y: 5)
     }
     .frame(width: 200, height: 200)
 }
 
 
 */
